[project]
name = "ohrm-exporter"
version = "0.0.1"
description = "Batch convert OHRM databases to RO-Crate JSON-LD and upload to Figshare"
license = "GPL-3.0-only"
requires-python = ">=3.12"
dependencies = [
    "docker~=7.0",
    "pyyaml~=6.0",
    "requests~=2.31",
]
